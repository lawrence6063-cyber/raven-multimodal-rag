"""
Embedding 模块 — 向量编码抽象与多 Provider 实现。

================================================================================
📖 Embedding 选型指南
================================================================================

在 RAG 系统中，Embedding（向量编码）决定了文本如何被转换为可检索的向量表示，
直接影响检索的召回率和精确度。以下从 **Provider 选型** 和 **编码策略** 两个维度
进行分析。

================================================================================
🏢 一、Embedding Provider（模型提供者）对比
================================================================================

┌─────────────────────┬──────────────────────────────────────────────────────────┐
│ Provider            │ 说明                                                     │
├─────────────────────┼──────────────────────────────────────────────────────────┤
│ 1. OpenAI           │ text-embedding-3-small / text-embedding-3-large          │
│    (云端 API) ✅     │ 优点：效果业界领先；支持自定义维度（256~3072）；          │
│                     │       API 简单稳定；社区生态最成熟。                      │
│                     │ 缺点：需联网；按 token 计费；数据出境合规风险。           │
│                     │ 适用：通用场景、快速原型、英文为主的文档。                │
│                     │                                                          │
│ 2. Azure OpenAI     │ 与 OpenAI 同模型，部署在 Azure 云上。                    │
│    (企业云端) ✅     │ 优点：企业级 SLA；数据不出境（选择国内区域）；            │
│                     │       与 Azure 生态（AD认证、VNet）深度集成。             │
│                     │ 缺点：需要 Azure 订阅；部署配置较复杂；                  │
│                     │       模型更新滞后于 OpenAI 官方。                        │
│                     │ 适用：企业生产环境、合规要求高的场景。                    │
│                     │                                                          │
│ 3. Ollama           │ 本地运行开源模型（nomic-embed-text、mxbai-embed-large    │
│    (本地部署) ✅     │ 等）。                                                   │
│                     │ 优点：完全离线；零 API 费用；数据隐私有保障；             │
│                     │       支持 GPU 加速。                                    │
│                     │ 缺点：需要本地 GPU 资源；模型效果通常弱于 OpenAI；        │
│                     │       需自行管理模型版本和服务可用性。                    │
│                     │ 适用：离线环境、隐私敏感场景、开发调试。                  │
│                     │                                                          │
│ 4. Cohere           │ embed-english-v3.0 / embed-multilingual-v3.0             │
│    (云端 API)        │ 优点：原生支持多语言；内置 search_document/query 类型；   │
│                     │       支持压缩（int8/binary）降低存储成本。               │
│                     │ 缺点：国内访问不稳定；生态不如 OpenAI 成熟。             │
│                     │ 适用：多语言检索、需要向量压缩的场景。                    │
│                     │                                                          │
│ 5. BGE / M3E        │ BAAI/bge-large-zh-v1.5、moka-ai/m3e-base 等。           │
│    (开源自部署)      │ 优点：中文效果优秀；可本地部署；模型小巧高效。           │
│                     │ 缺点：需要自建推理服务（如 TEI/vLLM）；                  │
│                     │       英文效果不如 OpenAI。                               │
│                     │ 适用：中文为主的文档、需要私有化部署。                    │
│                     │                                                          │
│ 6. Voyage AI        │ voyage-3 / voyage-code-3                                 │
│    (云端 API)        │ 优点：代码检索效果极佳；长上下文支持好。                 │
│                     │ 缺点：较新，社区支持有限；价格较高。                     │
│                     │ 适用：代码库检索、长文档场景。                            │
│                     │                                                          │
│ 7. Qwen (通义千问)  │ text-embedding-v3 / text-embedding-v2                     │
│    (阿里云 API) ✅   │ 优点：中文语义理解极强（原生中文训练）；国内访问稳定      │
│                     │       （DashScope，延迟<100ms）；数据不出境合规；          │
│                     │       兼容 OpenAI SDK 格式，切换成本极低；                 │
│                     │       性价比极高（约 0.0007元/千token）。                  │
│                     │ 缺点：纯英文效果略逊于 OpenAI；海外访问延迟较高。         │
│                     │ 适用：中文为主的知识库、国内部署、预算敏感场景。           │
└─────────────────────┴──────────────────────────────────────────────────────────┘

================================================================================
📊 二、主流 Embedding 模型性能对比
================================================================================

┌──────────────────────────────┬──────┬────────┬──────────┬─────────────────────┐
│ 模型                         │ 维度 │ 最大Token│ MTEB均分 │ 特点                │
├──────────────────────────────┼──────┼────────┼──────────┼─────────────────────┤
│ text-embedding-3-large       │ 3072 │ 8191   │ 64.6     │ OpenAI 最强，可降维  │
│ text-embedding-3-small       │ 1536 │ 8191   │ 62.3     │ 性价比最高           │
│ text-embedding-ada-002       │ 1536 │ 8191   │ 61.0     │ 旧版，逐步淘汰       │
│ voyage-3                     │ 1024 │ 32000  │ 67.4     │ 长上下文，效果顶尖   │
│ nomic-embed-text (Ollama)    │ 768  │ 8192   │ 62.4     │ 开源最佳之一         │
│ mxbai-embed-large (Ollama)   │ 1024 │ 512    │ 63.6     │ 本地高质量           │
│ bge-large-zh-v1.5            │ 1024 │ 512    │ —        │ 中文最佳开源         │
│ Cohere embed-v3              │ 1024 │ 512    │ 64.5     │ 多语言，支持压缩     │
│ Qwen text-embedding-v3      │ 1024 │ 8192   │ —        │ 中文最强云端API      │
│ Qwen text-embedding-v2      │ 1536 │ 8192   │ —        │ 稳定版，中文优秀     │
└──────────────────────────────┴──────┴────────┴──────────┴─────────────────────┘

注：MTEB 分数来自 Hugging Face MTEB Leaderboard，仅供参考，实际效果取决于具体数据。

================================================================================
🔀 三、编码策略（Encoding Strategy）对比
================================================================================

┌─────────────────────────┬────────────────────────────────────────────────────┐
│ 策略                    │ 说明                                               │
├─────────────────────────┼────────────────────────────────────────────────────┤
│ 1. 纯稠密编码           │ 仅生成 Dense Vector（语义向量）。                   │
│    (Dense Only)          │ 优点：实现简单；语义匹配能力强。                   │
│                         │ 缺点：对精确关键词匹配（如专有名词）效果差。        │
│                         │                                                    │
│ 2. 纯稀疏编码           │ 仅生成 Sparse Vector（如 BM25 权重向量）。          │
│    (Sparse Only)         │ 优点：精确关键词匹配；可解释性强。                 │
│                         │ 缺点：无法理解同义词和语义关联。                    │
│                         │                                                    │
│ 3. 双路编码 ✅          │ 同时生成 Dense + Sparse 向量。                      │
│    (Dense + Sparse)      │ 优点：兼顾语义理解和精确匹配；                     │
│                         │       为混合检索（Hybrid Search）提供数据基础；      │
│                         │       召回率显著优于单路方案。                       │
│                         │ 缺点：存储空间翻倍；索引构建时间增加。              │
│                         │                                                    │
│ 4. 多向量编码           │ 为同一文本生成多个向量（如 ColBERT 的 token 级向量）│
│    (Multi-Vector)        │ 优点：细粒度匹配，效果最佳。                       │
│                         │ 缺点：存储成本极高；检索复杂度大。                  │
└─────────────────────────┴────────────────────────────────────────────────────┘

================================================================================
🎯 四、本项目的选型决策
================================================================================

【Provider 选择】：OpenAI + Azure + Qwen + Ollama 四路并存
─────────────────────────────────────────────────────
1. **OpenAI**（默认）：开发阶段首选，API 简单、效果好、迭代快。
2. **Azure OpenAI**：生产环境首选，企业级 SLA + 数据合规。
3. **Qwen**（中文首选）：中文场景效果最佳，国内低延迟，数据合规。
4. **Ollama**：离线开发/隐私场景备选，零成本调试。

四者通过 EmbeddingFactory 工厂模式统一管理，改配置即切换，代码零修改。

【编码策略】：Dense + Sparse 双路编码
─────────────────────────────────────────────────────
- Dense 向量由本模块的 Embedding Provider 生成（语义理解）
- Sparse 向量由 BM25 编码器独立生成（精确匹配）
- 两路向量与 Chunk 原文一起原子化写入向量数据库
- 检索时通过 RRF 融合算法合并两路召回结果

【模型推荐】：
─────────────────────────────────────────────────────
- 开发/原型：text-embedding-3-small（1536维，性价比最高）
- 生产/高精度：text-embedding-3-large（可降维至 1024/1536 平衡效果与成本）
- 中文场景：Qwen text-embedding-v3（1024维，中文语义理解最强）
- 离线/隐私：nomic-embed-text（768维，Ollama 部署）

================================================================================
📁 五、模块结构
================================================================================

- base_embedding.py     : BaseEmbedding 抽象基类 + EmbeddingError 异常
- embedding_factory.py  : EmbeddingFactory 工厂类 + register_embedding 装饰器
- openai_embedding.py   : OpenAI Embedding 实现（默认方案）
- azure_embedding.py    : Azure OpenAI Embedding 实现（企业生产）
- qwen_embedding.py     : Qwen 通义千问 Embedding 实现（中文首选）
- ollama_embedding.py   : Ollama 本地 Embedding 实现（离线/隐私）

使用示例：
    from src.libs.embedding.embedding_factory import EmbeddingFactory
    embedding = EmbeddingFactory.create(settings)
    vectors = embedding.embed(["Hello world", "你好世界"])
    # vectors: [[0.012, -0.034, ...], [0.056, 0.078, ...]]

================================================================================
⚙️ 六、关键配置项（settings.yaml）
================================================================================

embedding:
  provider: "openai"                    # 可选: openai / azure / qwen / ollama
  model: "text-embedding-3-small"       # 模型名称（qwen用: text-embedding-v3）
  dimensions: 1536                      # 向量维度（qwen推荐: 1024）
  api_key: "sk-xxx"                     # API 密钥
  base_url: ""                          # 自定义 endpoint（可选）
  azure_endpoint: ""                    # Azure 专用
  deployment_name: ""                   # Azure 部署名
  api_version: "2024-02-01"             # Azure API 版本
"""